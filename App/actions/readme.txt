This folder is used handle all the action (Redux action) related contants and funtions.
Since we are using redux-saga, the the file reducerActions.js will contain a pure 
function which will eventually fire/trigger a (corresponding) saga action/generator function
a reducer state update.

actionTypes.js file contains constants that are used manage the redux store.